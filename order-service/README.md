# order-service

Minimal order service skeleton for the e-commerce project.

Endpoints:
- POST /api/orders - create order
- GET /api/orders - list orders

Default port: 8083
Database: configured to use MySQL at jdbc:mysql://localhost:3306/orderdb (update creds in application.yml)

