spring:
  application:
    name: payment-service

  datasource:
    url: jdbc:mysql://localhost:3306/paymentdb?createDatabaseIfNotExist=true
    username: root
    password: Sourabh@123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: 8090

# Payment Gateway Configuration (Mock for now)
payment:
  gateway:
    mock-enabled: true
    # In production, add real gateway credentials here
    # stripe:
    #   api-key: sk_test_xxxxx